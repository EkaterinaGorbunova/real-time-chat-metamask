Part of `chore/modernize` umbrella migration.

## Changes
- `eslint`: 8.10.0 -> 8.57.1 (latest 8.x, picks up many rule fixes and Node 20+ compat)

## Notes
Other dev deps were already on the latest patch within their current major:
- `@playwright/test` ^1.59.1 (latest)
- `tailwindcss` 3.4.19 (latest 3.x)
- `autoprefixer` 10.5.0, `postcss` 8.5.10 (latest)
- `ably` 1.2.50 (latest 1.x; will be replaced in Phase 2)
- `ethers` 5.8.0 (latest 5.x; will be upgraded in Phase 4)

Major bumps for `next`, `react`, `eslint-config-next` are deferred to Phase 3.

## Verification
- `npm run build` passes
- `npm run lint` clean (one pre-existing warning in pages/index.js, unrelated)
- `npx playwright test` 14/14 passes
